# Local development — copy to `.env` (ignored by git). Production build uses `.env.production`.
VITE_BACKEND_URL=http://localhost:8000

# Optional
# VITE_WS_URL=ws://localhost:8000
# VITE_TOKEN_URL=https://bitzx.io
